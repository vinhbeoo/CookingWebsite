@model ProjectLibrary.ObjectBussiness.Tag

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Delete Tag</h1>

<div class="container">
    <h3 class="text-center">Are you sure you want to delete this contest?</h3>
    <div class="row">
        <div class="col-md-6 offset-md-3">
            @using (Html.BeginForm("Delete", "Tag", FormMethod.Post, new { @class = "needs-validation", novalidate = "true", id = "deleteForm" }))
            {
                @Html.HiddenFor(model => model.TagId)

                <dl class="row border rounded p-3 mb-3 bg-light">
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.TagId)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.TagId)
                    </dd>
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.TagName)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.TagName)
                    </dd>
                </dl>

                <div class="text-center">
                    <div class="form-group d-inline-block mr-2">
                        <button type="button" class="btn btn-danger" onclick="confirmDelete()">Delete</button>
                    </div>
                    <div class="form-group d-inline-block">
                        @Html.ActionLink("Back to List", "Index", "Tag", null, new { @class = "btn btn-secondary" })
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function confirmDelete() {
            if (confirm("Are you sure you want to delete this Tag?")) {
                document.getElementById("deleteForm").submit();
            }
        }
    </script>
}
