@model ProjectLibrary.ObjectBussiness.Tag

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Edit Tag</h1>

<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            @using (Html.BeginForm("Edit", "Tag", FormMethod.Post, new { @class = "needs-validation", novalidate = "true" }))
            {
                <div class="row border rounded p-3 mb-3 bg-light">
                    @Html.HiddenFor(model => model.TagId)
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label>@Html.DisplayNameFor(model => model.TagName)</label>
                        @Html.EditorFor(model => model.TagName, new { htmlAttributes = new { @class = "form-control" } })
                        <span asp-validation-for="TagName" class="text-danger"></span>
                    </div>
                </div>
                <div class="text-center">
                    <div class="form-group d-inline-block mr-2">
                        <input type="submit" value="Save" class="btn btn-primary" />|
                    </div>

                    <div class="form-group d-inline-block">
                        @Html.ActionLink("Back to List", "Index", "Tag", null, new { @class = "btn btn-danger" })
                    </div>
                </div>
            }
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
